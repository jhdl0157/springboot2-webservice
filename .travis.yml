language: java
jdk:
  - openjdk11

branches:
  only:
    - master

# Permission Denied
before_install:
  - chmod +x gradlew

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      jhdl0157@naver.com
before_deploy:
  - zip -r yoon-springboot2-webservice *
  - mkdir -p deploy
  - mv yoon-springboot2-webservice.zip deploy/yoon-springboot2-webservice.zip

deploy:
  - provider: s3
    access_key_id: $AKIA33DBN5SFK2YBAG56 # Travis repo settings 에 설정된 값
    secret_access_key: $EO9hnTKNJHFoisgcBuDhxrjbeTaMOxss4jtmyBRV # Travis repo settings 에 설정된 값
    bucket: test-spring # S3 버킷
    region: ap-northeast-2
    skip_cleanup: true
    acl: private # zip 파일 접근을 private
    local_dir: deploy # before_deploy 에서 생성한 디렉토리
    wait-until-deployed: true